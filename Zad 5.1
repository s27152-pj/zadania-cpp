#include <iostream>
#include <vector>

using namespace std;

int main(){
    vector <int> vec;
    int we;
    int var;
    int i = 1;
    
    cout << "Jak jest dlugosc wektora: ";
    cin >> we;
    
    while (vec.size() != we){
        cout << "Podaj zmienna wektora " << i << ": ";
        cin >> var;
        vec.push_back(var);
        i++;
    }
    
    cout << "Twoj wektor to: ";
    
    for (int i = 0; i < vec.size(); i++){
        cout << vec[i] << " ";
    }
    cout << endl;
    
    int a = 0;
	int b = 0;
	int c = 0;
	
	for (int t = 0; t < vec.size(); t++){
		a = vec[t];
		if (a >= b){
		    c = t;
			b = a;
		}
	}
	
	cout << "Najwyzsza wartosc wektora to: " << b << endl;
	cout << "I znajduje sie na pozycji: " << c+1;
    
    return 0;
}
