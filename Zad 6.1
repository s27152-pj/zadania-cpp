#include <iostream>

using namespace std;

int main(){
    cout<<"Jak jest twoja liczba: ";
    int n;
    cin >> n;
    
    int p = 0;
    
    for (int i = 1; i <= n; i++){
        if ((i % 5) == 0 && (i % 3) != 0)
            p++;
    } 
    cout << p;
    return 0;
}
